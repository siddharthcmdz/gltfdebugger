import React from 'react'

const loadGlb = (glb) => {
    //use three to load the glb
    //parse the glb 
    //load relevant into into our model
}

const GdbModelTree = () => {

    //may be first look at list and ul
    //look into accordian later
    //look into drawer
  return (
    <div>GdbModelTree</div>
  )
}

export default GdbModelTree